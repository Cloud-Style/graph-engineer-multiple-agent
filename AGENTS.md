## Agent skills

### Issue tracker（问题跟踪）

Issues 与规格以 markdown 形式放在 `.scratch/<feature>/` 下。详见 `docs/agents/issue-tracker.md`。

### Domain docs（领域文档）

单上下文仓库：根目录 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
