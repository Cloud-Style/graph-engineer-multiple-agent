# 04 — 设计冻结人闸（暂停/恢复）

**What to build:** 冻结设计产生后，`run` 必须硬停并在 `RunResult` 中标明等待人闸；开发者可在 CLI/库上批准以恢复、或驳回以致失败结束。未批准前不得启动任何实现用写码 worktree。接缝测试覆盖暂停与恢复。

**Blocked by:** 03 — 模块设计扇出 + Reconciler（含冲突）

Status: ready-for-agent

- [ ] 设计冻结后进入可序列化的暂停态，产物与状态可从 run 目录/结果中读出「等人批准」
- [ ] 支持批准后恢复继续、驳回后以失败/中止结束
- [ ] 未批准前不创建实现用写 worktree/写分支
- [ ] `run` 接缝测试覆盖：停在人闸、批准后越过、驳回后不再进入实现阶段
