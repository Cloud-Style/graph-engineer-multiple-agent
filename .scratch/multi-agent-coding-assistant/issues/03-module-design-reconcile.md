# 03 — 模块设计扇出 + Reconciler（含冲突）

**What to build:** 在已有契约之上，`run` 按模块扇出 Module-Designers（遵守 v1 扇出上限）各自落盘模块设计；Reconciler 先做确定性冲突检出，再（可桩）LLM 调和，产出冲突报告与冻结设计；无冲突与有冲突两条路径都可通过接缝演示，且禁止静默吞掉冲突。

**Blocked by:** 02 — 编排 + 契约垂直切片

Status: done

- [x] Module-Designers 按契约中的模块列表扇出，每模块一份设计产物，且不修改目标仓生产代码
- [x] v1 模块扇出有明确上限，超出策略可测（截断、拒绝或升级——行为固定并写清）
- [x] Reconciler 对结构化字段做确定性 diff，冲突报告点名字段与模块
- [x] 存在「无冲突 → 直接冻结设计」与「有冲突 → 报告 + 调和/升级」两条可验证路径
- [x] 接缝测试覆盖上述路径；不得出现冲突被静默省略的情况
