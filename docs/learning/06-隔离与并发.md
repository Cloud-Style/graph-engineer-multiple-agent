# 06 — 隔离与并发（骨架）

## 要回答的问题

为什么「设计一致」仍可能在写码时打架？共享读、隔离写分别解决什么？worktree 把冲突推迟到了哪里？

## 计划覆盖

- 混合隔离策略
- 分支 / worktree 的收益与代价
- 当前串行实现 vs 真正并行扇出

## 对照代码（写正文时展开）

- `src/macs/worktree.py`
- `src/macs/pipeline.py`（`_implementers`）

状态：骨架。
