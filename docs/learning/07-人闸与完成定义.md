# 07 — 人闸与完成定义（骨架）

## 要回答的问题

哪些决策不该交给模型自动做完？「completed」到底承诺了什么？auto 模式改变了什么、没改变什么？

## 计划覆盖

- 设计冻结闸 vs 合入闸
- 完成 = PR 草稿 + 检查通过 + 人/auto 批准，≠ 合入 main
- `human` / `auto` 在审计中的记录方式

## 对照代码（写正文时展开）

- `src/macs/pipeline.py`（`_await_*`、`_handle_*`）
- `src/macs/run.py`（`auto_approve` 循环）

状态：骨架。
