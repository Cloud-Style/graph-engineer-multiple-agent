# 05 — 状态与持久化（骨架）

## 要回答的问题

一次多 Agent run 里，哪些是短暂执行状态？哪些是长期产物？代码改动存在哪？审计日志为什么要追加写？

## 计划覆盖

- `PipelineState` vs `runs/<id>/*.json` vs git worktree vs `events.jsonl`
- 为什么 resume 依赖磁盘而不是「Agent 还活着」
- 当前未使用 LangGraph 原生 checkpointer 的取舍

## 对照代码（写正文时展开）

- `src/macs/pipeline.py`（`_persist_status`）
- `src/macs/run.py`（`resume`）
- `src/macs/events.py`

状态：骨架。欢迎从「暂停后换终端再 resume」的实验笔记扩写。
